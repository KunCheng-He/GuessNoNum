# 1A2B 猜数字网页版开发计划 (本地同屏版)

## 1. 项目概述
**目标**：开发一个基于网页的**双人同屏 (Local Multiplayer / Hotseat)** 1A2B 猜数字游戏。
**核心体验**：两位玩家共用同一台设备（手机/平板/电脑），轮流操作完成游戏，无需任何网络连接。
**身份约束**：本计划由 Prometheus 制定，供执行者（Implementer）使用。

## 2. 技术架构选型
这是一个纯静态的前端应用，无任何服务端依赖。

- **核心模式**：**纯本地静态网页 (Static SPA)**
- **前端框架**：**Vue 3** (使用 Vite 构建)
  - *理由*：Vue 的响应式系统非常适合处理这种“回合制”的状态流转。
- **样式方案**：**Tailwind CSS**
  - *理由*：便于快速构建美观的交互界面（特别是模态框、遮罩层）。
- **数据存储**：**Pinia** 或 Vue `ref` (内存存储)
  - *理由*：游戏状态仅在当前会话有效，刷新即重置。

## 3. 核心交互流程 (UX Flow)

由于两人共用一屏，核心难点在于**“设置谜底”时的防偷窥设计**。

### 3.1 阶段一：设置谜底 (Setup Phase)
1.  **初始界面**：点击“开始游戏”按钮。
2.  **玩家 A 设置**：
    -   全屏遮罩弹出，提示文案：“**请玩家 B 转过头去/闭眼，仅限玩家 A 操作**”。
    -   玩家 A 点击“我已准备好” -> 显示数字输入键盘。
    -   输入 4 位不重复数字 -> 点击“确认并隐藏”。
    -   系统清除屏幕显示。
3.  **玩家 B 设置**：
    -   全屏遮罩弹出，提示文案：“**请玩家 A 转过头去/闭眼，仅限玩家 B 操作**”。
    -   玩家 B 输入 4 位不重复数字 -> 点击“确认并开始”。

### 3.2 阶段二：对战 (Battle Phase)
1.  **界面布局**：
    -   顶部：当前回合提示（如：“🔵 轮到 玩家 A 猜测”）。
    -   中部：历史记录列表（双列布局，左侧显示 A 的猜测记录，右侧显示 B 的猜测记录）。
    -   底部：数字输入键盘 + “提交猜测”按钮。
2.  **操作逻辑**：
    -   玩家 A 输入猜测 -> 提交 -> 系统计算 A/B 结果 -> 写入历史记录 -> 切换到玩家 B 回合。
    -   玩家 B 输入猜测 -> 提交 -> 系统计算 A/B 结果 -> 写入历史记录 -> 切换到玩家 A 回合。

### 3.3 阶段三：结算 (End Phase)
-   **触发条件**：某一方猜出的结果为 `4A0B`。
-   **胜利展示**：
    -   弹窗恭喜获胜方。
    -   同时揭晓双方的谜底数字。
-   **重玩**：点击“再来一局”，清空数据，回到阶段一。

## 4. 功能需求清单

### 4.1 逻辑功能
- [x] **数字验证器**：确保输入的是 4 位、纯数字、无重复字符。
- [x] **1A2B 算法**：`calculate(secret, guess) -> { A: number, B: number }`。
- [x] **状态机**：管理 `SETUP_A` -> `SETUP_B` -> `PLAYING` -> `FINISHED` 的状态流转。

### 4.2 UI 组件
- [x] **数字软键盘**：
    -   包含 0-9 数字键、退格键、确认键。
    -   *优化*：已输入的数字在键盘上置灰（可选，辅助玩家排除）。
- [x] **隐私遮罩层 (Privacy Curtain)**：用于切换玩家时的过渡，防止瞬间看到对方的操作。
- [x] **历史记录条**：清晰展示 `[回合数] 1234 -> 1A2B`。

## 5. 执行步骤 (Implementation Roadmap)

1.  **脚手架初始化**：
    -   `npm create vite@latest guess-number -- --template vue-ts`
    -   安装 Tailwind CSS。
    -   ✅ **已完成**
2.  **核心逻辑开发**：
    -   实现 `useGameLogic.ts` (Composable)，包含状态管理和算法。
    -   ✅ **已完成**
3.  **UI 开发**：
    -   开发 `SecretInput` 组件（含防偷窥遮罩）。
    -   开发 `GameBoard` 组件（猜测区 + 历史记录）。
    -   ✅ **已完成**
4.  **整合与测试**：
    -   模拟两人操作流程，验证体验是否流畅。
    -   ✅ **已完成**

## 6. 项目结构

```
/Users/hkc/Code/GuessNoNum/
├── .vscode/
│   └── extensions.json
├── dist/                    # 构建输出目录
│   ├── assets/             # 静态资源
│   └── index.html          # 入口文件
├── public/                  # 公共资源
│   └── vite.svg
├── src/                     # 源代码目录
│   ├── assets/             # 静态资源
│   │   └── vue.svg
│   ├── components/         # Vue 组件
│   │   ├── GameBoard.vue      # 游戏主面板（历史记录 + 输入区）
│   │   ├── PrivacyCurtain.vue # 隐私遮罩层组件
│   │   ├── SecretInput.vue    # 秘密数字输入组件
│   │   └── SoftKeyboard.vue   # 数字软键盘组件
│   ├── composables/        # 组合式函数
│   │   └── useGameLogic.ts # 游戏逻辑核心（状态管理 + 算法）
│   ├── App.vue            # 根组件
│   ├── main.ts            # 应用入口
│   └── style.css          # 全局样式
├── Plan.md                # 项目开发计划（本文档）
├── README.md              # 项目说明
├── package.json           # 项目配置
├── tailwind.config.js     # Tailwind 配置
├── tsconfig.json          # TypeScript 配置
└── vite.config.ts         # Vite 配置
```
